include_mail = false

exclude_path = [
    ".claude",
    "build",
    "tools/vendor",
    "vendor",
]

# Release tag URLs only resolve once the tag is pushed.
exclude = [
    'https://github\.com/NexusPHP/mcp-sdk/releases/tag/',
]
