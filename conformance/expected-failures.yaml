# Conformance scenarios this SDK does not pass yet.
#
# The referee exits 0 when the only failures are listed here, 1 on an unlisted
# failure, and 1 on a *stale* entry: a listed scenario that has started passing.
# That last rule is the point. It forces the list to burn down instead of rot.
#
# Baselined against the referee pinned in run-server.sh, at
# `--spec-version 2026-07-28 --suite all`. Bump the pin and reconcile this file
# in the same change, never separately.
#
# Entries are bare scenario names. Per-check granularity is `scenario:check-id`,
# and mind the space: `- scenario:check-id` is a string, `- scenario: check-id`
# is a mapping and is not what the referee reads. Wholesale and per-check
# entries for one scenario are contradictory and rejected.
#
# Every entry names what will retire it.

server:
  # Server-side MRTR (SEP-2322) is not built. `InputRequiredResult` is modelled in
  # Core/Schema and the client consumes it, but no server path emits it and
  # `ToolExecutorInterface::execute()` returns `CallToolResult` only. Retired by the
  # "Multi round-trip requests (MRTR)" serving work in ROADMAP.md.
  - input-required-result-basic-elicitation
  - input-required-result-basic-sampling
  - input-required-result-basic-list-roots
  - input-required-result-request-state
  - input-required-result-multiple-input-requests
  - input-required-result-multi-round
  - input-required-result-non-tool-request
  - input-required-result-result-type
  - input-required-result-tampered-state
  - input-required-result-capability-check

  # The same MRTR gap, reached through SHOULD-level checks rather than MUSTs. The
  # referee's exit code treats an unmet SHOULD exactly like a failure. Both want the
  # server to re-request via `InputRequiredResult` instead of answering with an
  # error, which is the serving half that does not exist.
  - input-required-result-missing-input-response
  - input-required-result-ignore-extra-params

  # A `pending` scenario, so it is outside the active and draft suites and only
  # runs under `--suite all`. `ParameterHeaderValidationMiddleware` needs the same
  # ToolStore the server serves, but `ServerBuilder::build()` takes `setToolStore()`
  # in preference to the tools `register()` collected, so a fixture built by
  # attribute discovery cannot also hand its store to `SecuredHttpEndpoint`.
  # Retired by giving the two a way to compose.
  - http-custom-header-server-validation

client:
  # These five are the residue of the OAuth block, and each needs its own look.
  # They are NOT the old loopback blocker, which is gone: every other auth
  # scenario now passes outright. Each of these gets most of the way and misses
  # one specific obligation, so they are listed individually rather than as a
  # group.
  #
  # Stops after Protected Resource Metadata, like the whole block used to. The
  # var2 metadata layout is evidently one discovery does not follow.
  - auth/metadata-var2
  # SHOULD use a URL-based client ID when the server advertises
  # `client_id_metadata_document_supported`. The harness supplies no
  # `clientIdMetadataDocumentUrl`, and a CIMD URL must be HTTPS, so this may not
  # be reachable from a loopback harness at all.
  - auth/basic-cimd:cimd-client-id-used
  # No second authorization request is made on a 403 insufficient_scope, so the
  # scope union on re-authorization cannot be observed either.
  - auth/scope-step-up
  # No token request is made. The harness passes the MCP server URL as the
  # pre-registered `issuer`, which is not the authorization server's issuer, so
  # this is more likely the harness than the SDK.
  - auth/pre-registration
  # SEP-2352 says the client MUST re-register when PRM `authorization_servers`
  # changes. No registration request is seen at the new server.
  - auth/authorization-server-migration:sep-2352-reregister-on-as-change

  # The MRTR round trip is driven through `Client::callTool()`'s `inputResponses`
  # and `requestState`, which now exist, but the harness registers no handler for
  # this scenario yet.
  - sep-2322-client-request-state

  # An unmet SHOULD. When a server rejects the requested protocol version with
  # `-32022`, the client should retry using one the error named as supported. It
  # does not: the version is a readonly property and every typed method stamps it
  # into a request that is already built by the time the rejection arrives, so a
  # retry means rebuilding the request at dispatch level.
  - request-metadata:sep-2575-client-retry-supported-version
